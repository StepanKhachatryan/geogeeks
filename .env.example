
# Payment gate. The committed defaults already make the tool paid.
# NEXT_PUBLIC_PAYMENT_REQUIRED=false          # turns the converter free again
# NEXT_PUBLIC_UNLOCK_ENDPOINT=https://...     # overrides the verifier URL
# NEXT_PUBLIC_REQUEST_ENDPOINT=https://...    # overrides the request/status URL
# NEXT_PUBLIC_UNLOCK_PRICE=2000 AMD           # shown above the phone field
